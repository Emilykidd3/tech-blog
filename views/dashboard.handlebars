    <a href="/dashboard/newpost">Create a new post</a>
{{#if posts.length}}
<section>
    <h2>Your Posts</h2>

    <ul>
        {{#each posts as |post|}}
        <li>
            {{> post-info post}}
            </br>
        </li>
        {{/each}}
    </ul>
</section>
{{/if}}
